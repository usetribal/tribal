#!/bin/sh
# Lineage post-commit hook: link recently imported sessions to the new commit.
set -e

export PATH="${HOME}/.cargo/bin:${PATH}"

if command -v tribal >/dev/null 2>&1; then
  tribal hook post-commit
elif tribal hook post-commit; then
  :
fi
